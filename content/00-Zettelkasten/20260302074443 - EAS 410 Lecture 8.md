---
created: "2026-03-02 at 07:44"
tags:
  - fleeting
publish: "false"
---
> The end of this lecture he went over the end of chapter 5, I missed this section because I was late. He then proceeded to begin chapter 6
---
- There are a number of semiconductor memory types, UV curing mentioned as a way to clear the data on a memory cell.
- RAM is separated into it's dynamic and static RAM. DRAM requires periodic charge refreshing because it uses the presence or abscence of charge in a cell. The key component is a cap and the transistor acts a switch.
- Note that SRAM requires significantly more components which can limit its size. Because of this, DRAM has higher density. The issue is that you need refresh circuit but it scales easily so it doesn't pose a significant disadvantage.
- Both SRAM and DRAM are volatile.![[Pasted image 20260302075752.png|center]]
- Any physicial structure that allows you to encode it that doesn't need a power source to maintain bits is a ROM
- However a PROM is similar but it can only be written onto once. This is useful for high volume production runs.
- Read mostly memory is designed to be read from more frequently than it is to be written to and it includes things like *EPROM, EEPROM, Flash Memory*
- Interleaved memory arranges memory banks and each bank can independently serve read writes **simultaneously**. 
- Error corrrections may emerge from hard or soft failures. The hard errors may emerge from hardware failure while the soft efforts may originate from power supply noise or any *alpha particles*. Soft errors are not going to cause permanent damage to the memory.
- Silicon in the light for instance creates unpredictability.
- To perform error correction, we store the input and an associated function. This means that we store more bits than absolutely necessary but then we store into the memory and compares the function result at two different stages to verify that the data that came in the data that we still have. If an issue is detected, send an error signal.
- The system will either be able to fix the issue, only if there was only 1 bit flip and then it would run through a corrector that gives the data out otherwise we send an error signal and call it a day.
- Hamming Codes try to have the data physically as far apart as possible. The spheres help us determine the number of bits that we need to inject to reach our error correcting capacity. 
- We are looking for even parity have even parity.
- Use Error Correction when you have larger data sizes.
- We use a syndrome vector to help us determine whether or not there is an error. When we *XOR* this with out data then we will be able to correct the bit where there is an issue. ![[Pasted image 20260302081957.png|center]]
- When considering the layout of the data bins, we place our check bits in the locations where only 1 of the bits in its address is a 1 uniquely. So this happened to be in the positions, $1,2,4,8$, Each checkbit covers a region of the data . 
- Note all codes can handle more than 1 error and so error correction code design is an interesting way to go.
- *SEC-DEC* this is a single error correcting, double error detecting codes are an industry standard. We just want to know if two errors happened. It introduces an additional bit that ensures that the parity of the whole sphere region. If two errors would have occured, then it would break the parity of the outer bit. ![[Pasted image 20260302085124.png|center]]
- Error detection has much less overhead than error correction.
- Synchronized DRAM, is different in that it is treated as a main part of the processor and works to the clock of the processor and since they share the smae clock then you no longer require wait states.
- Note *Figure 6.12* that describes the SDRAM functional units and the pin assignments in the next table.
- **Cache Latency, time between receiving data and being able to read out.**
- **Burst Length, defines the amount of cycles required to send the data out each time it happens**
- DDR Data Rate SDRAM, the Double Data Rate originates from the fact that it operates on both the rising and falling edge of the clock doubling the number of events that can trigger an event. This is unlike standard SDRAM that only operates on the rising edge.
- Typically lower voltages will be able to facilitate higher frequencies, this is useful when comparing the trends between DDR generations. 
- Embedded DRAM is application focused and can be used to replace cache, DRAM is denser. Remember locality, if we had used SRAM, we can only use so many bits because its wider. So we opt for DRAM because its denser and allows us to pack more bits close to our processor. This is regardless of the fact that eDRAM has a higher cost per bit.
- Flash memory is cool, the main benefit is that you can remove a specific block of memory without wiping the entire chip. It only uses a single transistor per bit and so it has an insane density.![[Pasted image 20260302090155.png|center]]
- Flash memory pretty much takes advantage of quantum tunnelling and traps electrons under the guise of memory.
- There are 2 Flash strucutres, NAND and NOR. NAND is more common. NOR cells all run in parallel, it is interesting in that the decision is probabalisitic. NAND cells operate in parallel. We need 8 of these strucutres, to store 1 byte, but review this because i don't understand how it scales.
- Flash pretty much works by appling a test voltage to ascertain the value in a certain structure and then applying another higher voltage outside our target threshold.
- SRAM is closest to the CPU within the memory hierarchy.
- TLC and QLC both mean 3, 4 bit storage per transistor respectively. This is incredibly dense but super slow because of the amont of probing with voltages that we need to do. So we normally pair this with a high speed 1 bit SRAM that can  be used in the immediacy of things and then it could be moved into TLC and QLC strucutres during the free moments.